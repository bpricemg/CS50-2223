#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Silly because we could do this with a for loop instead
    //int x = 0;
    //while (x < 6)
    //{
    //    printf("hello, world\n");
    //    x++;
    //}

    // This is the for loop equivalent
    for (int i = 0; i < 6; i++)
    {
        printf("hello, world\n");
    }

}